<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRUD">
        <activity android:name=".Reporte3"></activity>
        <activity android:name=".Reporte2" />
        <activity android:name=".Reporte1" />
        <activity android:name=".ConsultaClientes" />
        <activity android:name=".ConsultaAutos" />
        <activity android:name=".MenuReportes" />
        <activity android:name=".CrudServicios" />
        <activity android:name=".CrudClientes" />
        <activity android:name=".CrudAutos" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CRUD.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>